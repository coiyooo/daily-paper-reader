---
title: Autonomously Acquiring Robot Manipulation Skills with Language-Driven Quality-Diversity
title_zh: 基于语言驱动的质量-多样性算法自主获取机器人操作技能
authors: "Émiland Garrabé, Mahdi Khoramshahi, Stéphane Doncieux"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30983v1"
tags: ["query:param-skills"]
score: 8.0
evidence: 自主质量多样性算法生成多样化运动原语库，支撑操作技能复用
tldr: 质量多样性（QD）算法能生成运动原语库以适应部署约束，但通常需专家设计成功条件、适应度和多样性指标，阻碍机器人自主性；现有LLM奖励塑形则仅输出单一方案。本文提出语言驱动的自主QD方法，将策略探索视为低维功能设计，利用LLM采样无需任务特定提示，并采用多BD MAP-Elites Success算法。在Genesis模拟器的4个操纵任务上生成多样化运动原语，优于经典QD算法，仅凭自然语言描述即可自主获得适应性与多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.30983v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30983v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 279, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30983v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 873, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30983v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 737, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30983v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 648, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.30983v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 758, \"height\": 188, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30983v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 760, \"height\": 191, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30983v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 726, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30983v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 730, \"height\": 188, \"label\": \"Table\"}]"
motivation: 现有QD需专家设计指标、LLM奖励塑形仅输出单一解，导致机器人难以自主获得多样且可适应的技能。
method: 将策略探索作为低维功能设计，用LLM采样适应度和行为描述符函数，并采用多BD MAP-Elites Success生成运动原语档案。
result: 在Genesis模拟器的4个机器人操纵任务上，生成多样化运动原语库，性能优于推断和手写参数化的经典QD算法。
conclusion: 仅需任务的自然语言描述，即可自主生成多样化技能库，提升机器人部署时的适应性与自主性。
---

## 摘要
质量-多样性（QD）算法在机器人学习领域日益受到关注，其中多样化的运动基元库使机器人能够在部署时零样本适应约束条件。然而，这类方法通常需要专家设计者编写成功条件、适应度与多样性指标，这极大地限制了机器人的自主性。另一方面，现有基于大语言模型（LLM）的奖励塑形技术虽然允许机器人自主学习，但仅能输出单个高性能解，限制了机器人的适应性。本文提出一种方法，通过自主利用质量-多样性算法输出多样化的运动基元库，仅需以自然语言提供任务的无格式描述即可。为解决设计相关适应度与多样性指标的难题，我们提出一种自主探索机制，能够可靠地输出覆盖适应度与行为描述符（BD）空间的函数集。首先，我们将策略探索构建为函数设计问题，其中函数空间的维度低于完整的BD与适应度空间，并提出一种基于LLM的探索方案，无需任何任务特定提示、微调或专家干预即可从这些低维空间中进行采样。我们改进了MAP-Elites成功（MES）算法的多BD变体，以利用异构的BD样本。最后，通过基于Genesis模拟器的实验，我们展示了所提方法能有效生成多样化的运动基元库，在一组4个机器人操作任务上，优于使用推断和手写参数化的经典QD算法。

## Abstract
Quality-diversity (QD) algorithms have been gaining traction in robot learning, where diverse motion primitive libraries allow robots to adapt zero-shot to constraints at deployment time. However, such methods typically require expert designers to write the success condition, fitness and diversity metrics, and this strongly limits the robot's autonomy. On the other hand, existing LLM-based reward-shaping techniques allow robots to learn autonomously but only output single high-performing solutions, limiting the robot's adaptability. In this paper, we propose an approach designed to output diverse motion primitive archives by autonomously leveraging quality-diversity algorithms, only requiring a free-form description of the task in common language. To address the difficulty of designing relevant fitness and diversity metrics, we propose an autonomous exploration mechanism able to reliably output sets of functionals covering the fitness and behavior descriptor (BD) space. First, we pose policy exploration as a functional design problem, where the functional spaces are lower-dimensional than the full BD and fitness spaces, and propose an LLM-based exploration scheme to sample from these low-dimensional spaces without any task-specific prompts, fine-tuning or expert intervention. We adapt a multi-BD variant of the MAP-Elites success (MES) algorithm, designed to leverage the heterogeneous BD samples. Finally, through experiments based on the genesis simulator, we show that our method effectively generates archives of diverse motion primitives, outperforming classical QD algorithms with inferred and hand-written parametrizations on a set of $4$ robotic manipulation tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何让机器人仅凭自然语言任务描述，自主获取**多样化**的操作技能库，而非单一高性能策略。
- **关键矛盾在于两类现有方法的各自短板**：
  - **质量-多样性（QD）算法**（如 MAP-Elites）：能够生成多样化的运动基元档案，使机器人可零样本适应部署时的未知约束，但要求专家手工设计成功条件、适应度函数和多样性指标（BD），限制了机器人自主性。
  - **基于 LLM 的奖励塑形方法**（如 Eureka 等）：允许机器人自主学习，但仅输出单一高性能策略，缺乏应对未知任务实例所需的适应性。
- **本文的定位**：将 LLM 的能力与 QD 算法结合，不需要任务特定提示、微调或专家干预，仅凭自然语言描述即可自主生成多样化的运动基元档案。

## 2. 论文提出的方法论

### 2.1 核心思想

- 将策略探索重新构建为**低维函数设计问题**：不直接探索完整的 BD 空间（不可实现），而是将 BD 函数分解为两个更简单的函数空间的组合：
  - **信息提取函数** \( g: I \to \mathbb{R}^\infty \)，决定从仿真中提取什么物理量；
  - **时间戳选择函数** \( h: \mathbb{I}^N \to I \)，决定在什么时间时刻提取该物理量。
- 最终 BD 为 \( g \circ h \)，即"在特定时刻测量特定物理量"。
- 通过 LLM **无放回采样**，用少量函数样本覆盖这些低维空间，构成 **BD mesh**。

### 2.2 算法流程（Algorithm 1）

共四个步骤：

1. **推断阶段（LLM inference）**：
   - 推断 1 个成功条件 \( sc \)；
   - 推断 3 个适应度函数（单次推理生成，无放回采样）；
   - 推断 4 个时间戳函数 \( h \)（单次推理，无放回）；
   - 推断 4 个测量量函数 \( g \)（单次推理，无放回）；
   - 组合生成 BD mesh \( \phi = \{g_k \circ h_j\} \)（共 16 个候选 BD）。
2. **适应度探索**：为每个适应度函数各创建 1 个多 BD 档案，短时间训练后比较各档案的成功个体数。
3. **适应度选择**：保留成功个体最多的档案对应的适应度函数。
4. **档案生成**：用选定的适应度函数和 BD mesh 进行完整的多 BD MES 训练，输出最终档案。

### 2.3 多 BD MAP-Elites Success（MES）变体

- 对每个 BD 维护一个独立的网格（archive grid）。
- 每个个体在评估后，同时插入**所有** BD 网格，进行局部竞争。
- 选择阶段：从每个网格中各自选出父代个体，**拼接**后统一进行变异和评估。
- 这一机制使算法同时从多条多样性流中获益：驱动型 BD（dense）快速填充成功个体，描述型 BD（sparse）保证功能差异。

## 3. 实验设计

### 3.1 仿真环境

- 使用 **Genesis 仿真引擎**（支持 GPU 并行化）。
- 候选解模板：6-DoF 初始位姿（3D 位置 + 3D 朝向）加 3-DoF 笛卡尔运动向量，执行线性抓取运动。
- 感知 API 向 LLM 提供：目标位置/朝向、接触信息、夹爪状态、关节角度等。

### 3.2 任务（共 4 个操作任务）

1. **抓取杯子（Grasp the mug）** ：要求物体接触夹爪且不接触桌子。
2. **打开热水龙头（Turn on the hot water）** ：要求水龙头旋转至少 0.314 弧度。
3. **拉开抽屉至少 10 cm（Open the drawer）** ：要求抽屉移动超过 10 cm。
4. **在桌上滑动杯子至少 5 cm（Slide the mug）** ：要求杯子在桌子上移动至少 5 cm。

每个任务都预定义了人工编写的地面真值 BD（用于评估和有监督基线）。

### 3.3 对比基线

- **Ours**：本文方法（LLM 推断 + 多 BD MES）。
- **Naive inference**（直接推断）：标准 MES + LLM 推断的适应度和 BD，但不用本文的探索过程。
- **Hand-crafted MES**（人工 MES）：标准 MES + 专家手写成功条件、适应度与 BD。
- **Genetic Algorithm（GA）** ：标准遗传算法，仅使用 LLM 推断的适应度，无多样性机制。

### 3.4 评估指标

- **成功档案大小**：输出档案中成功个体的数量（用专家手写的成功条件判定）。
- **Ground-truth BD 覆盖率**：将档案映射到手工定义的 GT BD 空间，衡量功能多样性。

## 4. 资源与算力

- 文中**未具体报告** GPU 型号、数量或训练时长。
- 仅提及：使用 GPT-4o（temperature=0）进行推理；使用 Genesis 仿真器，借助 **GPU 并行化**快速评估个体；每代包含评估多个个体，实验共 50 代。
- 因未提供硬件规格，无法评估训练成本的具体规模，这是论文在资源披露方面的不足之处。

## 5. 实验数量与充分性

### 实验规模
- 4 种操作任务 × 4 种方法 × 每组重复 5 次（以 mean ± std 报告）。
- 每方法每任务 50 代演化。

### 充分性与公正性评估
- **优点**：计算量匹配（相同种群规模与评估次数），对比公平；多种任务类型覆盖了稀疏奖励、稀疏交互等不同探索难度；统计形式报告（mean ± std）合理。
- **不足**：
  - **无消融实验**：未分析 BD mesh 中 BD 数量、函数组合方式的影响；未单独验证时间戳与信息函数拆分的贡献。
  - **任务范围有限**：4 个均为简单运动原语级别的操作任务，较复杂的长程操作任务不在范围内。
  - **单候选解模板**：仅使用线性运动和固定 6-DoF 初始位姿，未对比更丰富的轨迹表达。
  - **缺乏 sim2real 迁移实验**。

## 6. 论文主要结论与发现

- **核心结论**：本文提出的语言驱动 QD 方法能够自主生成多样化的运动基元档案，仅需自然语言任务描述，且在 4 个操作任务中均优于三种基线。
- **关键发现 1 — 多 BD 优于单一 BD**：手工描述型 BD 空间稀疏，导致经典 MES 档案较小；推断出的 BD 往往是驱动型（较稠密），能促进早期发现成功个体。多 BD 并行维护"两种世界的优势"——既快速填充成功个体，又保有 GT BD 空间的覆盖率。
- **关键发现 2 — 推动型 BD 在短周期的优势**：在 50 代内，推断 BD 反而优于手工"描述型" GT BD（因为后者在稀疏奖励下难以快速获得成功个体）。
- **关键发现 3 — GA 基线无效**：在稀疏交互任务中，缺少多样性机制的遗传算法表现极差，说明 QD 方法在操作技能获取中的必要性。
- **潜在趋势**：作者指出若实验周期足够长，手工描述型 BD 可能最终在 GT BD 覆盖上反超，但本文的多 BD 方法可同时受益。

## 7. 优点

- **高自主性**：只需自然语言任务描述，无需专家干预、任务特定提示或微调，实现端到端自主技能库生成。
- **层次化探索设计**：将难以直接搜索的高维 BD 空间分解为"时间戳 × 信息量"的低维组合，思想简洁且有可扩展性。
- **多 BD 网格并行架构**：避免了单个 BD 选择的"押注"风险，使算法同时受益于多种多样性度量。
- **问题形式化清晰**：提出了运动基元档案设计的数学形式（Problem 1），将 QD 算法参数化问题建立在坚实的形式化基础上。
- **适应性强**：生成的档案天然支持"对未知任务实例的鲁棒性"，与操作技能复用与下游规划的 integration 相契合。
- **实验设计比较公平**：与手工基线、直接推断基线和普通 GA 计算量匹配。

## 8. 不足与局限

- **实验广度不够**：仅 4 个仿真任务，全部为"简单线性抓取/推拉"类短时操作，缺乏多步骤、长时程和复杂接触操作验证。
- **未做消融研究**：无法量化各组件（如 BD 数量、无放回采样、适应度筛选机制）的独立贡献。
- **高方差问题**：实验数据显示（如抓手成功数 403.3±218.0），算法对随机初始化较敏感，稳定性不足。
- **固定候选解模板**：6-DoF 初始位姿+线性运动向量的假设过于局限，难以覆盖更复杂的操作需求，也限制了所提方法适用于更广泛任务的可能性。
- **无 sim2real 评估**：这是仿真中的方法与真实世界部署之间的关键差距。
- **未报告算力开销**：LLM 推断次数、成本与仿真训练的具体算力需求未说明，但可推测多次 LLM 推理是额外开销。
- **多 BD 缺少与更优手工多 BD 基线的对比**：作者也承认，缺乏多 BD 手工设计的最佳实践，因此没有该基线，无法严格区分"多 BD 结构"与"LLM 采样"各自贡献了多大优势。

**（完）**

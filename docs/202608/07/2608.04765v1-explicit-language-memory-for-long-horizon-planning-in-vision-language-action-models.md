---
title: Explicit Language Memory for Long-Horizon Planning in Vision-Language-Action Models
title_zh: 显式语言记忆用于视觉-语言-动作模型中的长时程规划
authors: "Houze Xu, Jizhong Li, Ziyi Ye"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04765v1"
tags: ["query:param-skills"]
score: 8.0
evidence: 显式语言记忆的分层长视野VLA，支持跨任务知识复用与组合泛化
tldr: 现有VLA模型在长时程任务中面临跨任务泛化弱、时序一致性差、误差累积和语义表征退化等问题。提出分层长时程VLA架构，引入显式语言记忆模块，将时序观测转换为带时间逻辑的文本记忆序列。高层VLM进行语义推理并递归更新记忆与子任务指令，低层VLA执行连续控制。实验表明该方法提升复杂长时程任务的成功率与鲁棒性，并提供可解释的决策语义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1542, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 765, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1597, \"height\": 1048, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1330, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 210, \"label\": \"Table\"}]"
motivation: 现有VLA模型因稀疏示范和马尔可夫假设，难以处理长时程任务，且端到端微调削弱高层语义，需显式记忆机制。
method: 提出分层架构，高层VLM用VQA范式更新语言记忆和子任务指令，低层VLA基于指令与视觉观测执行控制。
result: 在多个仿真环境及真实机器人平台上，显式语言记忆显著提升长时程任务成功率与鲁棒性。
conclusion: 显式语言记忆为VLA模型提供时序推理与错误修正能力，同时增强决策可解释性。
---

## 摘要
视觉-语言-动作（VLA）模型为连接视觉感知、语言理解和机器人控制提供了统一范式。然而，现有VLA模型在长时程任务中仍面临重大挑战：稀疏的专家演示限制了跨任务的组合泛化能力；长时程任务的非马尔可夫性质使得仅以当前观测为条件的策略难以维持时间一致性；有限的闭环纠错能力导致执行误差不断累积；端到端的动作微调可能削弱视觉语言模型（VLM）骨干网络的高层语义表征。为解决这些问题，我们提出了一种带有显式语言记忆模块的分层长时程VLA架构。核心思想是将离散的时间观测转化为具有时间逻辑的连贯文本记忆序列。系统被解耦为高层VLM和低层VLA：高层VLM通过视觉问答训练范式进行语义推理，低层VLA则在子任务指令和视觉观测的条件下执行精确的连续控制。高层VLM利用先前的记忆作为上下文锚点，递归更新语言记忆和子任务指令，从而在长时程执行过程中实现持续的时间跟踪和动态纠错。我们在多个仿真环境中评估了所提方法，并在真实机器人平台上进行了仿真到真实迁移实验。结果表明，显式语言记忆提高了VLA模型在复杂长时程任务上的成功率和鲁棒性，同时为决策过程提供了可解释的语义说明。

## 速览
**TLDR**：现有视觉-语言-动作(VLA)模型在长时程任务中面临稀疏演示导致泛化弱、非马尔可夫性引起时序不一致、误差累积以及微调削弱语义表征等挑战。为此提出一种带显式语言记忆模块的分层VLA架构，将离散时序观测转为带时序逻辑的文本记忆序列，由高层VLM通过视觉问答递归更新记忆和子任务指令，低层VLA执行连续控制。在仿真与真实机器人上的实验表明，该方法显著提升长时程任务的成功率和鲁棒性，并提供可解释的语义决策过程。 \
**Motivation**：现有VLA模型在长时程任务中受稀疏演示、非马尔可夫性、误差累积和语义弱化限制，难以保持时序一致与组合泛化。 \
**Method**：提出分层VLA架构，高层VLM以视觉问答方式递归更新显式语言记忆和子任务指令，低层VLA基于子任务与视觉执行连续控制。 \
**Result**：在多个仿真环境和真实机器人平台上，显式语言记忆提升了长时程任务的成功率和鲁棒性，同时决策过程可解释。 \
**Conclusion**：显式语言记忆为长时程VLA规划提供了有效的时序锚定与纠错机制，兼顾高性能与语义可解释性。

---

## Abstract
Vision-language-action (VLA) models provide a unified paradigm for connecting visual perception, language understanding, and robotic control. However, existing VLA models still face major challenges in long-horizon tasks: sparse expert demonstrations constrain cross-task compositional generalization; the non-Markovian nature of long-horizon tasks makes it difficult for policies conditioned only on current observations to maintain temporal consistency; limited closed-loop error correction allows execution errors to accumulate; and end-to-end action fine-tuning may weaken the high-level semantic representations of vision-language model (VLM) backbones. To address these issues, we propose a hierarchical long-horizon VLA architecture with an explicit language-memory module. The central idea is to convert discrete temporal observations into a coherent textual memory sequence with temporal logic. The system is decoupled into a high-level VLM and a low-level VLA: the high-level VLM performs semantic reasoning through a visual question answering training paradigm, while the low-level VLA executes precise continuous control conditioned on subtask instructions and visual observations. The high-level VLM recursively updates both language memory and subtask instructions using the previous memory as a contextual anchor, enabling persistent temporal tracking and dynamic correction during long-horizon execution. We evaluate the proposed method in multiple simulation environments and conduct sim-to-real experiments on a real robotic platform. The results demonstrate that explicit language memory improves the success rate and robustness of VLA models on complex long-horizon tasks while providing an interpretable semantic account of the decision process.

---

## 论文详细总结（自动生成）

# 论文总结：显式语言记忆用于视觉-语言-动作模型中的长时程规划

## 1. 核心问题与整体含义（研究动机与背景）

- **研究背景**：视觉-语言-动作（VLA）模型将视觉感知、语言理解和机器人控制统一为单一策略范式，是具身智能的重要方向。
- **核心问题**：现有VLA模型在长时程（long-horizon）任务中面临四大挑战：
  1. **稀疏示范限制组合泛化**——专家演示数据稀缺，难以支撑跨任务的组合式泛化。
  2. **非马尔可夫性导致的时序不一致**——长时程任务中，仅凭当前观测无法判定任务进度（如"是否已抓取成功""按钮是否已按下"），可能造成跳过阶段、重复已完成动作或在未观测到失败时直接执行后续动作。
  3. **闭环纠错能力有限**——执行误差随动作序列逐步累积，缺乏及时修正机制。
  4. **端到端动作微调削弱语义表征**——动作微调可能破坏VLM骨干网络的高层语义表达能力。
- **整体意义**：论文提出以**显式自然语言记忆**作为紧凑的历史表征，将长时程策略分解为"高层语义规划 + 低层连续控制"，以可解释的方式提升VLA模型在长时程任务中的阶段一致性和鲁棒性。

## 2. 方法论（核心思想、技术细节、流程）

### 2.1 核心思想：分层策略 + 显式语言记忆

- 将长时程策略分解为两个耦合的子策略：
  - **高层VLM（π_HL）**：基于全局指令 l、当前观测 o_t 和前一记忆 m_{t-1}，递归更新语言记忆并生成简洁子任务指令 s_t。
  - **低层VLA（π_LL）**：基于当前观测 o_t 和子任务指令 s_t，利用流匹配动作专家生成固定视界（H=30）的连续动作块 a_{t:t+H}。
- 整体策略概率分解：
  \[
  \pi(a_{t:t+H}, s_t, m_t \mid o_t, m_{t-1}, l) = \pi_{LL}(a_{t:t+H} \mid o_t, s_t) \cdot \pi_{HL}(s_t, m_t \mid o_t, m_{t-1}, l)
  \]

### 2.2 递归语言记忆（Recursive Language Memory）

- 记忆 m_t 是**结构化自然语言摘要**，记录：已完成里程碑（过去时）、决策相关的对象属性与状态、观测到的失败、下一个待完成意图。
- **滚动压缩规则**：子任务完成前保留有助于识别目标的细节（类别、颜色、位置等）；完成后压缩为简短的过去时陈述，并将注意力转移至下一步。例如：
  - "I am moving to the red radio on the coffee table." → "I moved to the radio, and I am going to pick up the red radio."
- 只有当当前观测提供活动里程碑变化的证据时，高层模型才更新记忆。

### 2.3 自动化记忆监督（Automated Memory Supervision）

- 利用专家视频 + 技能级标注（技能名、描述、起止时间戳）生成训练目标。
- 通用VLM（Gemini 3.5 Flash API 或本地 Qwen3.5-9B）基于当前帧与完整技能标注，生成：
  1. 简洁子任务指令（形式：动作 + 目标对象 + 上下文，无主语）；
  2. 第一人称语言记忆（过去时描述已完成技能 + 未来态过渡语句）。
- 提示工程避免实现相关术语（如"帧""序列""技能索引"），保证记忆的自然性。

### 2.4 独立分支训练

- **高层分支**：PaliGemma（3B VLM），自回归训练，预测更新后的记忆与子任务，目标函数：
  \[
  \mathcal{L}_{HL} = -\mathbb{E}\left[\log \pi_{HL}^m(m_t \mid o_t, m_{t-1}, l) + \log \pi_{HL}^s(s_t \mid o_t, m_{t-1}, l)\right]
  \]
- **低层分支**：沿用π0.5的动作专家，流匹配动作目标函数：
  \[
  \mathcal{L}_{LL} = \mathbb{E}\left[\| a_{t:t+H} - \pi_{LL}(o_t, s_t) \|_2^2\right]
  \]
- **独立训练的优势**：避免在单一目标中手工调节语义生成与连续控制的损失平衡。

### 2.5 闭环推理流程

- 初始化记忆 m_0 = ∅。
- 循环执行：获取观测 → 高层更新记忆与子任务 → 低层生成动作块 → 执行 → 判断终止。
- 记忆与子任务均以可见文本输出，形成可解释的决策日志，可对齐动作失败。

## 3. 实验设计（数据集/场景、基准、对比方法）

- **基准方法**：以原生 π0.5 策略为基线，对比"π0.5 + 显式语言记忆"。
- **评价指标**：阶段级成功率（Stage-wise Success Rate），平均各阶段完成率，比仅报告最终任务成功率更能定位失败阶段。

### 3.1 BEHAVIOR-1K：开启收音机（家庭场景仿真）

- 任务：根据指令"打开客厅桌子上的收音机"，完成"走向收音机→拿起→按按钮→放回桌上"四阶段。
- 四个计分阶段：MoveTo、PickUp、PressOn、PlaceOn。

### 3.2 Genie Sim 3.0：包裹分拣（人形操作仿真）

- **单包裹任务**：跟随包裹→拾取→放入扫描区（条码朝上）→再次拾取→放入蓝色箱子→回到初始朝向，共6个计分阶段（Follow、PickUp-1、Inside-1、Upright、PickUp-2、Inside-2）。
- **连续分拣任务**：按任意顺序分拣4个不同颜色的包裹，每个包裹"条码朝上放入扫描区"计为完成一个阶段。

### 3.3 真实 XLeRobot 平台：抓取与放置（真实机器人）

- 平台：双SO-ARM 101机械臂 + LeKiwi移动底座 + RGB头摄像头 + 手部相机。
- 任务：从桌上拿起方块放到盘子里，计分阶段：Follow、PickUp、MoveTo、Place。
- 数据规模：25条人工遥操作轨迹，评估20次试验。

## 4. 资源与算力

- **训练**：4张 NVIDIA A100 GPU（论文明确提及）。
- **推理**：1张 NVIDIA RTX 5880 Ada Generation GPU（论文明确提及）。
- **训练配置**：LoRA适配高层PaliGemma与低层VLA的VLM骨干，300M参数的Gemma动作专家全量微调；优化器为AdamW，峰值学习率 2.5×10⁻⁵，批大小 64，训练步数 50,000。
- **动作块长度**：H = 30。
- 论文未提及具体训练时长（如小时数/天数）。

## 5. 实验数量与充分性

- **实验组数**：3个场景（2个仿真 + 1个真实），每个场景报告基线与带记忆模型的对比结果，共约6组主要对比。
- **充分性评价**：
  - **优点**：覆盖家庭仿真、人形仿真与真实机器人，验证了方法的多场景适用性；连续分拣任务检验了长期重复性；真实实验验证了sim-to-real迁移。
  - **不足**：
    1. **无消融实验**——未单独验证记忆模块各组件（如滚动压缩、证据条件更新）的贡献。
    2. **无统计显著性**——未报告随机种子间的方差或置信区间，结论的统计稳定性未知。
    3. **真实实验规模小**——仅25条训练轨迹、20次评估，且任务较短（4个阶段），推广性有限。
    4. **基线对比单薄**——仅对比π0.5原生策略，未与同类记忆增强方法（如MemoryVLA、MAP-VLA等）对比。

## 6. 主要结论与发现

1. **显式语言记忆提升长时程任务成功率**：
   - BEHAVIOR-1K：平均成功率 30.0%→40.0%，PickUp阶段增益最大（10%→30%），PressOn仍然困难。
   - Genie Sim单包裹：41.7%→63.9%，Follow与首次PickUp翻倍，Inside-1从50.0%→83.3%。
   - Genie Sim连续分拣：31.3%→46.9%，带记忆模型在12.5%评估中完成全部4个包裹放置，基线无法达到第3、4次放置。
   - 真实平台：62.5%→66.3%，增益较小（各阶段 +5%），符合"短任务中历史信息作用有限"的预期。
2. **记忆提升阶段一致性**：带记忆模型仅在观测支持当前里程碑完成时才推进阶段，减少了跳过关键步骤（如按按钮、条码扫描）的错误。
3. **语义规划与运动控制分离**：PressOn的低成功率说明语言记忆主要解决"做什么/处于哪个阶段"，不直接改善接触丰富的运动控制（如按钮定位、力控制）。
4. **支持情境内失败恢复**：文本失败状态可条件化后续决策，实现简单的重抓取等恢复行为，无需单独恢复控制器。
5. **可解释性**：记忆与子任务均为可见文本，便于定位决策失败是高层次语义错误还是低层控制错误。

## 7. 优点（方法与实验亮点）

- **架构设计清晰**：高层VLM / 低层VLA解耦，语义规划与连续控制独立优化，避免多目标平衡难题。
- **紧凑的历史表征**：滚动压缩语言记忆避免拼接全部历史帧，控制上下文与计算成本，且可解释。
- **自动化监督流水线**：从专家视频与技能时间戳自动生成帧对齐的记忆/子任务标注，降低了标注成本。
- **阶段级评价指标**：报告阶段成功率而非仅最终成功率，能精确定位长序列中的失败环节。
- **覆盖多场景验证**：同时涵盖家庭仿真、人形仿真和真实低成本机器人，提升了结论的泛化可信度。
- **可解释决策日志**：记忆与子任务的文本输出为分析决策过程提供了直观依据。

## 8. 不足与局限

- **实验覆盖有限**：
  - 真实平台仅短时程（4阶段）简单任务，未验证分钟级、多对象、多次交互或环境变化的真实场景。
  - 仿真实验未报告随机种子方差或置信区间，缺乏统计显著性分析。
- **对比方法单一**：仅与π0.5原生基线对比，未与历史感知类方法（HAMLET、MemoryVLA、MAP-VLA等）或推理增强方法（EmbodyCoT、dVLA等）对比。
- **计算开销增加**：分层架构需在低层动作生成前额外调用高层VLM，增加了端到端延迟与部署复杂度。
- **接触丰富任务仍受限**：PressOn等需要精确几何对齐与力控的任务成功率极低，说明语言记忆无法弥补低层控制数据不足或精度缺陷。
- **失败恢复能力有限**：可靠恢复依赖收集包含失败与纠正动作的多样化演示数据，当前数据覆盖不足。
- **记忆模态单一**：仅使用语言记忆，未融合触觉、力反馈等多模态信号，对视觉上模糊的状态（如接触成功与否）判别力有限。

## （完）
